"""
    This folder implements anything which is related to flows.
    Specifically, it implements a flow-wrapper which takes as input two distributions,
    and then tries to train on both (is this actually a good idea lol?).
"""